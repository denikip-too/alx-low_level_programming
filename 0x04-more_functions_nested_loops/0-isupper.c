#include "main.h"
/**
* _isupper - checks for uppercase characters
* Description: function that checks for uppercase character
* @c: interger
* Return: 0
*/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
  return (1);
return (0);
}
