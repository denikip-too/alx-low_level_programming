#include "main.h"
/**
* mul - function that multiplies two integers
* @a: first integer
* @b: second integer
* Return: multiplication of two integers
*/
int mul(int a, int b)
{
int multipication;
multiplication = a * b;
return (multiplication);
}
